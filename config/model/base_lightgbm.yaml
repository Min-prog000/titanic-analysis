data:
  scaler: MinMaxScaler

model:
  # num_leaves: 10
  max_depth: 5
  learning_rate: 0.001
  # subsample: 0.5
  n_estimators: 1000 # num_iterations
  colsample_bytree: 0.9
  # reg_alpha: 0.1
  # reg_lambda: 5
  random_state: 0
  # early_stopping_round: 10
  verbosity: -1
