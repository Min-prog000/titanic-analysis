data:
  scaler: MinMaxScaler

model:
  # num_leaves: 20
  max_depth: 5
  learning_rate: 0.001
  min_child_samples: 30
  # subsample: 0.5
  n_estimators: 2000 # num_iterations
  objective: "binary"
  colsample_bytree: 0.8
  # reg_alpha: 1
  # reg_lambda: 5
  random_state: 0
  # early_stopping_round: 10
  verbosity: -1
  metric: "logloss"
