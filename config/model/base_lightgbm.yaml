data:
  scaler: MinMaxScaler

model:
  # num_leaves: 20
  max_depth: 5
  learning_rate: 0.01
  subsample: 0.2
  n_estimators: 100 # num_iterations
  colsample_bytree: 0.8
  # reg_alpha: 0.1
  # reg_lambda: 5
  random_state: 0
  # early_stopping_round: 10
  verbosity: -1